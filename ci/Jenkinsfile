pipeline {
    agent {
        label 'DockerAgents'
    }
    environment {
        SONARQUBETOKEN = credentials('SonarQube')
    }
    options {
        disableConcurrentBuilds()
    }
    stages {
        stage('Run CLI Command') {
            steps {
                script {
                    println "Running build"
                }
            }
        }
        stage('Maven Package') {
            steps {
                mavenPackage()
            }
        }
        stage('Sonar') {
            steps {
                sh "mvn sonar:sonar \
                      -Dsonar.projectKey=com.aeontree.springci \
                      -Dsonar.host.url=http://localhost:13004 \
                      -Dsonar.login=$SONARQUBETOKEN"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}

def mavenPackage() {
    def maven_build="mvn clean package"
    sh(script: maven_build)
}