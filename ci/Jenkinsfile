pipeline {
    agent {
        docker {
            image 'dockeragentmvn'
            label 'DockerAgents'
            args '-v maven_m2:/root/.m2'
        }
    }
    stages {
        stage('Run CLI Command') {
            steps {
                script {
                    println "Running build"
                }
            }
        }
        stage('Maven Package') {
            steps {
                mavenPackage()
            }
        }
        stage('List Maven Repository Contents') {
            steps {
                sh 'ls /root/.m2'
            }
        }
    }
}

def mavenPackage() {
    def maven_build="mvn clean package"
    sh(script: maven_build)
}